export function GET_CURRENT_CHAT(state) {
  return state.selectedChat;
}

export function GET_CURRENT_USER(state) {
  return state.currentUser;
}

export function GET_CURRENT_CHATS(state) {
  return state.currentChats;
}
